<% if @course.errors.any? %>
    <div class="box">
        <p><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</p>
        <ul>
        <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
<% end %>

<div class="field">
    <%= f.label :title, class: "label" %>
    <div class="control">
        <%= f.text_field :title, autofocus: true, class: "input" %>
    </div>
</div>

<div class="field">
    <%= f.label :description, class: "label" %>
    <div class="control">
        <%= f.text_area :description, class: "input" %>
    </div>
</div>

<div class="field">
    <%= f.label :student_emails, 'Students', class: "label" %>
    <div class="control">
        <%= f.text_area :student_emails, class: "input" %>
    </div>
</div>

<div class="field">
    <div class="control">
        <%= f.submit submit, class: "button is-primary" %>
    </div>
</div>
